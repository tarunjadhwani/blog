%h1 Listing blogs

%table
  %thead
    %tr
      %th Title
      %th Subtext
      %th Content
      %th Published date
      %th
      %th
      %th

  %tbody
    - @blogs.each do |blog|
      %tr
        %td= blog.title
        %td= blog.subtext
        %td= blog.content
        %td= blog.published_date
        %td= link_to 'Show', blog
        %td= link_to 'Edit', edit_blog_path(blog)
        %td= link_to 'Destroy', blog, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Blog', new_blog_path
